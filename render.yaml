services:
  - type: web
    name: procurement-portal
    runtime: node
    buildCommand: npm install && npm install --prefix frontend && frontend/node_modules/.bin/vite build --root frontend
    startCommand: node backend/server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
